[package]
name = "modelo"
version = "1.0.0"
description = "Templates de texto: variáveis, laços, condicionais e filtros."
authors = ["DataForge"]
license = "MIT"
entry = "src/main.df"
dataforge = ">=1.0"
keywords = ["template", "texto", "render"]

[dependencies]

[scripts]
test = "test tests/"
