[package]
name = "tentativa"
version = "1.0.0"
description = "Repetição com recuo exponencial, jitter e circuit breaker."
authors = ["DataForge"]
license = "MIT"
entry = "src/main.df"
dataforge = ">=1.0"
keywords = ["retry", "resiliencia", "backoff"]

[dependencies]

[scripts]
test = "test tests/"
