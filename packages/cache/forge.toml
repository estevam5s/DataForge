[package]
name = "cache"
version = "1.0.0"
description = "Cache: memoização, TTL e LRU, com estatísticas de acerto."
authors = ["DataForge"]
license = "MIT"
entry = "src/main.df"
dataforge = ">=1.0"
keywords = ["cache", "memoizacao", "ttl"]

[dependencies]

[scripts]
test = "test tests/"
