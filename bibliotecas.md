math — matemática
cmath — matemática complexa
decimal — precisão decimal
fractions — frações
statistics — estatística
random — números aleatórios
numbers — tipos numéricos
numpy — computação numérica e arrays
scipy — computação científica
sympy — matemática simbólica
pandas — DataFrames e análise de dados
polars — processamento de dados
datetime — datas e horários
zoneinfo — fusos horários
calendar — calendários
time — tempo
os — sistema operacional
sys — sistema/runtime
pathlib — caminhos
shutil — operações de arquivos
glob — busca de arquivos
tempfile — arquivos temporários
fileinput — entrada de arquivos
json — JSON
csv — CSV
sqlite3 — SQLite
pickle — serialização
struct — dados binários
base64 — Base64
hashlib — hashes
hmac — HMAC
secrets — segurança/aleatoriedade
ssl — SSL/TLS
socket — sockets
http — HTTP
urllib — URLs/HTTP
email — e-mail
smtplib — SMTP
asyncio — programação assíncrona
threading — threads
multiprocessing — processos
concurrent — concorrência
subprocess — processos externos
queue — filas
collections — estruturas de dados
itertools — iteradores
functools — funções utilitárias
operator — operadores
typing — tipagem
dataclasses — estruturas de dados
enum — enumerações
abc — classes abstratas
inspect — introspecção
ast — árvores sintáticas
re — expressões regulares
string — manipulação de strings
textwrap — texto
unicodedata — Unicode
logging — logs
warnings — avisos
traceback — stack traces
argparse — CLI
cmd — comandos interativos
configparser — configuração
pydoc — documentação
unittest — testes
doctest — testes em documentação
pytest — testes avançados
debugpy — debugging
async frameworks — servidores assíncronos
Flask — Web
Django — Web completo
FastAPI — APIs
SQLAlchemy — ORM
Pydantic — validação
Requests — HTTP
httpx — HTTP assíncrono
BeautifulSoup — HTML
Scrapy — scraping
Celery — jobs/filas
Redis — cache/mensageria
PyMongo — MongoDB
psycopg — PostgreSQL
OpenCV — visão computacional
Pillow — imagens
Matplotlib — gráficos
Seaborn — visualização
Scikit-learn — Machine Learning
TensorFlow — IA
PyTorch — IA
Transformers — modelos de IA
Jupyter — notebooks
PySpark — Big Data
Dask — processamento paralelo
Airflow — pipelines/ETL
BeautifulSoup — parsing HTML
Cryptography — criptografia
JWT — autenticação
OAuth — autenticação/autorização
fs — arquivos
path — caminhos
http — HTTP
https — HTTPS
http2 — HTTP/2
net — TCP
tls — TLS
dgram — UDP
dns — DNS
url — URLs
querystring — query strings
stream — streams
buffer — dados binários
events — eventos
process — processos
child_process — subprocessos
worker_threads — threads
cluster — clusterização
crypto — criptografia
zlib — compressão
assert — assertions
test — testes
timers — temporizadores
readline — terminal
repl — REPL
console — console
os — sistema operacional
util — utilidades
v8 — engine/runtime
vm — execução isolada
perf_hooks — performance
async_hooks — contexto assíncrono
diagnostics_channel — diagnóstico
inspector — debugging
trace_events — tracing
report — relatórios
sqlite — SQLite
webcrypto — Web Crypto
webstreams — Web Streams
wasi — WebAssembly/WASI
module — sistema de módulos
typescript — integração TypeScript
ffi — integração nativa
N-API — addons nativos