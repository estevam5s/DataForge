action classificar(n):
    given n bigger 0:
        yield "positivo"
    orif n smaller 0:
        yield "negativo"
    otherwise:
        yield "zero"

cycle n in [5, -3, 0]:
    out $"{n} -> {classificar(n)}"