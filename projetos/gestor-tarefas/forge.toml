[project]
name = "gestor-tarefas"
version = "1.0.0"
description = "Gestor de tarefas de linha de comando — junta 6 bibliotecas do registro."
entry = "src/main.df"
dataforge = ">=4.1"

[dependencies]
argumentos = "^1.0.0"
tabela = "^1.0.0"
datas = "^1.0.0"
cofre = "^1.0.0"
colecao = "^1.0.0"
registro = "^1.0.0"

[scripts]
start = "run src/main.df"
test = "test tests/"
