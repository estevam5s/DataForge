[project]
name = "analise-vendas"
version = "1.0.0"
description = "Lê CSV de vendas e produz um relatório com estatística e gráficos de terminal."
entry = "src/main.df"
dataforge = ">=1.0"

[dependencies]
estatistica = "^1.0.0"
colecao = "^1.0.0"
tabela = "^1.0.0"
moeda = "^1.0.0"
datas = "^1.0.0"
progresso = "^1.0.0"

[scripts]
start = "run src/main.df"
test = "test tests/"
